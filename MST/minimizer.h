#include <set>
#include <string>
#include <vector>
#include <deque>
#include <iostream>

using namespace std;

int resort(deque<string> const& W, string& m);
vector<int> minimizers(string const& S, int k, int w);

